/** Biblioteca, não Framework, para criação de interfaces.
 *
 * No MVC o React está na camada V, View.
 *
 * ReactJS -> Interfaces WEB. ReactDOM somado com React, manipula a DOM.
 *            Tudo é javascript, JSX, conexão de Javascript com XHML.
 *
 * React -> Modelo de componentização. Dividir em vários componentes que tem sentido sozinhos.
 *
 * React Native -> Somado com React, transforma em interface Nativa.
 *
 * Babel -> Transpilador que converte o código javascript em uma forma que o navegador entenda.
 *
 * Webpack -> Automatiza o processo de transpilação.
 *
 *  */
